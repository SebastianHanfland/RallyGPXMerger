import { Button } from 'react-bootstrap';

export function RemoveDataButton() {
    return (
        <Button variant="danger" onClick={() => {}}>
            Remove all data
        </Button>
    );
}
