import { DescriptionInfoButton } from '../DescriptionInfoButton.tsx';

export const TrackDescriptionInfoButton = () => {
    return (
        <DescriptionInfoButton titleMessageId={'msg.tracks.title'} descriptionMessageId={'msg.description.tracks'} />
    );
};
